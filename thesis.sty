% pdf内のハイパーリンクの設定
% コンパイル時間が倍増するため, 基本はコメントアウト推奨
\AtEndOfPackage{
  \usepackage[dvipdfmx,
    bookmarks=true,% ブックマークを作成
    bookmarksnumbered=true,% しおりのレベル
    colorlinks=true,% false: boxed links; true: colored links
    linkcolor=blue,
    %sitecolor=blue,
    urlcolor=blue,
    setpagesize=false,
    hyperfootnotes=false,
	pdfborder={0 0 0},
    pdftitle={master_thesis_teramura},% text for PDF Title field
    pdfauthor={Natsu Teramura},% text for PDF Author field
    pdfcreator={Natsu Teramura},% text for PDF Creator field
    pdfproducer={Natsu Teramura},% text for PDF Producer field
    pdfsubject={修士論文},   % text for PDF Subject field
    ]{hyperref}%
  \usepackage{pxjahyper}%
  \usepackage{multirow}
  \usepackage{subcaption}
  %\usepackage[a4paper]{geometry}
}

% 目次の深さ
\setcounter{tocdepth}{2}

%\renewcommand{\figurename}{Fig.}
%\renewcommand{\tablename}{Table}

% 参照を楽にするコマンド
\def \figref #1{図\ref{#1}}
\def \tbref #1{表\ref{#1}}
\def \equref #1{式(\ref{#1})}
\def \chapref #1{第\ref{#1}章}
\def \appendixref #1{付録\ref{#1}}
\def \secref  #1{\ref{#1}節}
\def \subsecref  #1{\ref{#1}節}
\def \subsubsecref  #1{\ref{#1}節}

\def\thesis #1{\gdef\@thesis{#1}}
\def\teacher #1{\gdef\@teacher{#1}}
\def\studentId #1{\gdef\@studentId{#1}}
\def\major #1{\gdef\@major{#1}}
\def\organization #1{\gdef\@organization{#1}}

\renewcommand{\maketitle}{
    \thispagestyle{empty}
    \begin{center}
        \vspace*{0.5cm}
        \LARGE{\@thesis} 
        \hspace*{\fill}\leavevmode\\
        \vspace{2.2cm}
        \LARGE{\@title}
        \hspace*{\fill}\leavevmode\\
        \label{font_title}
        \Large
        \vspace{0.2cm}
        \rightline{\@date}
        \hspace*{\fill}\leavevmode\\
        \vspace{0.6cm}
        \large{\@major} 
        \hspace*{\fill}\leavevmode\\
        \large{\@studentId}
        \hspace*{\fill}\leavevmode\\
        \large{\@author}
        \hspace*{\fill}\leavevmode\\
        \vspace{5cm}
        \large{\@organization}
        \hspace*{\fill}\leavevmode\\
    \end{center}
    \clearpage
    \mbox{}
    \thispagestyle{empty}
    \clearpage
}